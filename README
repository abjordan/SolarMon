To set up the environment the first time:

$ virtualenv venv
$ . venv/bin/activate
$ pip install -r requirements.txt

To turn on the server:

$ . venv/bin/activate
$ python SolarMon.py

To turn on the Vivint Logger:

$ . venv/bin/activate
$ python VivintLogger.py

